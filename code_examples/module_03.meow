void ShowPerson(name string) () (
     this.name = name;    
);

class Person (
     name string,
     surname string,
     age int,
     ShowPerson(string) (),
);

var persona = ^^Person(
     name = "John",
     surname = "Doe",
     age = 18,
);

var persona2 = ^^Person(
     name = "Jane",
     surname = "Doe",
     age = 15,
);
meow(persona.name);
meow(persona2.name);

persona.ShowPerson("Jane");
persona2.ShowPerson("John");

meow(persona.name);
meow(persona2.name);


## няшный комментарий