var radius = 1;
const pi = 3.14;

void findS(rad int) (int) (
    return rad * rad * pi;
);

for (radius != 10) (
    radius = radius + 1;
    meow(findS(radius));
);

meow("timeless journeys always are familiar...");