import math "code_examples/module_02.meow";

var radius = 1;
const pi = 3.14;
var i = 0;

void findS(rad int) (int) (
    return rad * rad * pi;
);

for (radius != 10) (
    radius = radius + 1;
    ## meow(findS(radius));
    meow(math.something[i]);
    i = i + 1;
);
