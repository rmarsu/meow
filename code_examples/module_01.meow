import math "code_examples/module_02.meow";

var radius = 1;
const pi = 3.14;
var i = 0;
var array = [1, 2, 3, 4, 5 + 1];

class human (
    static string name,
    int age,
    string honk(int),
);

void honk(int) (string) (
    return human.age;
);

var human1 = ^^human(
    name = "n",
    age = 100,
);

for (i != 5) (
    i = i + 1;
);

meow(human1.honk(1));

meow(math.findS(2));

