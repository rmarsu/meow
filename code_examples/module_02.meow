var i = 0;
var word = "hello world";
var a = ["letters"];

for (i < len(word)) (
    if (word[i] == " ") (
        meow("!! Пустой символ на " + string(i + 1));

    );
    a = tail(a, word[i]);
    i = i + 1;
);

void meowKawaii(arr array) (string) (
    var res = "";
    res = res + arr[0] + " ~"; 
    j = 1;
    for (j < len(arr)) (
        res = res + arr[j] + "~";
        j = j + 1;
    );
    return res;
);

meow("the result is " , meowKawaii(a));
